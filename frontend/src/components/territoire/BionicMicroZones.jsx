/**
 * BionicMicroZones.jsx — BCE-4X-MAX Phase 3.3-U-PRIME: PIPELINE V5 NEUTRALISE
 *
 * Le rendu V5 est DEFINITIVEMENT DESACTIVE.
 * Export BIONIC_MODULES conserve pour compatibilite (ZoneFavorites, etc.).
 */

import { BIONIC_MODULES } from '@/core/bionic';

export { BIONIC_MODULES };

const BionicMicroZones = () => null;

export default BionicMicroZones;
